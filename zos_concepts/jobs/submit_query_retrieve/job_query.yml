---
- name: Query z/OS Jobs
  hosts: zos_host
  collections:
    - ibm.ibm_zos_core
  gather_facts: false
  environment: "{{ environment_vars }}"

  tasks:
    - name: Set query parameters
      ansible.builtin.set_fact:
        query_params:
          job_name: "{{ job_name if (job_name is defined and job_name != '' and job_name != '*') else omit }}"
          owner: "{{ owner if (owner is defined and owner != '' and owner != '*') else omit }}"
          job_id: "{{ job_id if (job_id is defined and job_id != '' and job_id != '*') else omit }}"
          status_type: "{{ status_type if (status_type is defined and status_type != '' and status_type != 'all') else omit }}"

    - name: Query z/OS jobs
      ibm.ibm_zos_core.zos_job_query: "{{ query_params }}"
      register: job_query_result

    - name: Display job query results
      ansible.builtin.debug:
        msg: |
          ========================================
          z/OS Job Query Results
          ========================================
          Total jobs found: {{ job_query_result.jobs | length }}
          
          {% for job in job_query_result.jobs %}
          ---
          Job Name: {{ job.job_name }}
          Job ID: {{ job.job_id }}
          Owner: {{ job.owner }}
          Status: {{ job.ret_code.msg | default('ACTIVE') }}
          Return Code: {{ job.ret_code.code | default('N/A') }}
          Class: {{ job.class | default('N/A') }}
          {% if job.subsystem is defined %}
          Subsystem: {{ job.subsystem }}
          {% endif %}
          {% if job.svc_class is defined %}
          Service Class: {{ job.svc_class }}
          {% endif %}
          {% if job.priority is defined %}
          Priority: {{ job.priority }}
          {% endif %}
          {% if job.program_name is defined %}
          Program: {{ job.program_name }}
          {% endif %}
          {% endfor %}
          ========================================

    - name: Save results to file
      ansible.builtin.copy:
        content: "{{ job_query_result | to_nice_json }}"
        dest: "/tmp/job_query_result_{{ lookup('pipe', 'date +%s') }}.json"
      delegate_to: localhost

    - name: Summary
      ansible.builtin.debug:
        msg: "Query completed. Found {{ job_query_result.jobs | length }} job(s) matching criteria."

# Made with Bob
